<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_margin="0dp">
    <ImageView
        android:id="@+id/view_attribute_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingTop="0dp"
        android:paddingRight="6dp"
        tools:src="@drawable/ic_launcher"/>

    <TextView
        android:id="@+id/view_attribute_value"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@id/view_attribute_icon"
        android:paddingStart="0dp"
        android:paddingEnd="0dp"
        android:paddingTop="0dp"
        android:paddingBottom="0dp"
        android:textColor="#000000"
        android:textSize="32sp"
        tools:text="Day 5"/>

    <!-- paddingStart="1dp" aligns left edge of view_attribute_name with view_attribute_value -->
    <TextView
        android:id="@+id/view_attribute_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/view_attribute_value"
        android:layout_alignStart="@id/view_attribute_value"
        android:paddingStart="1dp"
        android:paddingEnd="0dp"
        android:paddingTop="0dp"
        android:paddingBottom="0dp"
        android:textColor="#000000"
        android:textSize="16sp"
        tools:text="Since admission" />

</RelativeLayout>
